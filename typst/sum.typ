Sum
$sum_(i=1)^n a_i$

Sum over set elements
$sum_(w in W) w dot f(w)$

Product
$product_(i=1)^n a_i$

Product over set elements  
$product_(w in W) (f(w) + 1)$

Exists with constraint
$exists n: W -> NN_0 : (forall w in W: n(w) <= f(w)) and (sum_(w in W) w dot n(w) = p)$
