# Sets

set notation
`{1,2,3}`

element of
`in`

not element of
`in.not`

intersection / Schnitt
`inter`

union
`union`

set difference
`without` or `∖`

subset
`subset`

subset or equal
`subset.eq`

natural numbers
`NN` or `bb(N)`

empty set
`emptyset`

Cartesian product (R squared)
`RR^2` or `bb(R)^2`

Infinity symbol
`oo`

Power set
`cal(P)(M)`
